apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kubepilot-postgres-pvc
  namespace: kubepilot
  labels:
    app.kubernetes.io/name: kubepilot
    app.kubernetes.io/component: postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
