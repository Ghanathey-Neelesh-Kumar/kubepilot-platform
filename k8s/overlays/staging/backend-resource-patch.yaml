apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubepilot-backend
  namespace: kubepilot
spec:
  template:
    spec:
      containers:
        - name: backend
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
