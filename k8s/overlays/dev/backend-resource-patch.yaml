apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubepilot-backend
  namespace: kubepilot
spec:
  template:
    spec:
      containers:
        - name: backend
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
