apiVersion: v1
kind: Service
metadata:
  name: broken-targetport-service
  namespace: kubepilot-dev
  labels:
    app.kubernetes.io/name: kubepilot
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kubepilot
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 5000
      targetPort: 9999